# Repository layer: focused modules extracted from the database.py monolith.
# Each module owns a single domain of business logic while sharing the
# common infrastructure (db_query, db_manager, etc.) from app.database.
