# Local secrets

Create `grafana_admin_password` in this directory before starting the stack. Its
contents must be a unique administrator password. Files in this directory are
ignored except this README and `.gitignore`.
