# Local secrets

Create `grafana_admin_password` in this directory before starting the stack and
set its mode to `0640` with owner/group `root:root`. Its
contents must be a unique administrator password. Files in this directory are
ignored except this README and `.gitignore`.
